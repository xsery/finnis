using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bonus : MonoBehaviour {

    //public Player Finnis;
    private GameObject raioValidaBonus;

    void OnMouseDown()
    {
        /*if (Finnis.pegoBonus)
        {
            Time.timeScale = 1;
            Finnis.pegoBonus = false;
            Finnis.raioValidaBonus = 0.2f;
            Finnis.toque = 0;
            Destroy(raioValidaBonus);
            gameObject.SetActiveRecursively(false);
        }*/
    }

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
